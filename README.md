# FERBenchmarkingPlatform

To recreate the environment, run this terminal cmmd:

conda env create -f env.yml


Run `pip install -r CustomModel/requirements.txt` or `conda env create -f CustomModel/env.yml`.
